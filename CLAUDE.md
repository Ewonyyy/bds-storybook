@AGENTS.md

# 프로젝트 개요

이 저장소는 팀이 Claude Code로 협업해 만드는 결과물을 위한 것입니다.

**최종 목표 (다음 단계, 지금은 아님):**
1. shadcn/ui를 베이스로 우리 팀만의 독자적인 컴포넌트 라이브러리(디자인 시스템) 구축
2. 그 라이브러리를 활용해 실제 웹을 구축

**지금 단계 범위:** 팀이 함께 Claude Code로 작업하기 위한 기본 세팅만. 디자인 시스템 구현, 레포/린트/패키지매니저 표준화, Figma MCP 연동은 아직 하지 않습니다.

## 팀 구성

- 각 팀원이 자신의 노트북에서 Claude Code CLI를 직접 실행
- 결과물은 이 GitHub 저장소에 공유, 서로의 작업을 수정/추가하며 협업

## 권한 정책 (`.claude/settings.json`)

- 읽기, 파일 편집/생성, 검색 등 안전한 작업은 **자동 승인**됩니다.
- `git push`, `git reset`, `rm` 등 되돌리기 어려운 작업은 **확인을 요청**합니다. 확인 프롬프트가 뜨면 내용을 확인하고 승인해주세요.
- 강제 push(`git push --force`)와 `rm -rf /*` 같은 위험한 명령은 아예 차단되어 있습니다.

## 세션 시작 자동화 (SessionStart hook)

세션을 켤 때마다 `.claude/hooks/session-start.py`가 자동 실행되어:
1. 로컬 Claude Code CLI 버전과 최신 버전을 비교해 업데이트가 필요하면 알려줍니다.
2. 세션 시작 시 자동으로 불러올 skill이 있다면 안내합니다.

**아직 자동 로딩할 skill이 없습니다.** 나중에 팀 전용 skill(예: 디자인 시스템 컴포넌트 생성 skill)이 만들어지면, `.claude/settings.json`의 `env.SESSION_START_SKILL` 값에 skill 이름을 채워주세요. 다음 세션부터 자동으로 로딩 안내가 나타납니다.

## 모델 사용 가이드

작업 종류에 따라 `/model` 명령으로 모델을 바꿔가며 쓰는 것을 권장합니다. Claude Code는 작업 내용에 따라 모델을 자동으로 바꾸지 않으므로, 아래 기준으로 직접 선택해주세요.

| 작업 유형 | 권장 모델 | 이유 |
|---|---|---|
| 일반적인 UI/컴포넌트 작업, Figma MCP 연동(추후), 간단한 질의응답, 문서 작성 | **Sonnet 5** (기본값) | 속도와 품질의 균형이 좋아 대부분의 일상 작업에 적합 |
| 복잡한 아키텍처 결정, 어려운 버그/구조적 문제, 애매하거나 리스크가 큰 판단이 필요한 작업 | **Opus 4.8** | 더 깊은 추론이 필요한 고난도 작업에 적합 |
| 아주 가볍고 반복적인 작업(파일 나열, 단순 포맷 확인 등) | **Haiku 4.5** | 빠르고 저렴하게 처리 가능 |

세션 도중 막히거나 결과 품질이 아쉬우면 `/model opus`로 전환 후 다시 시도해보세요.

## 워크플로 (팀원용 최소 가이드)

1. Claude Code를 실행하면 위 SessionStart 안내(버전/skill)를 먼저 확인합니다.
2. 작업을 요청하면, 읽기/편집은 자동 승인되고 `git push`처럼 위험한 작업만 확인을 요청합니다.
3. 작업이 끝나면 Claude Code에게 커밋을 요청하세요 (예: "이 변경사항 커밋해줘"). Claude Code가 무엇을, 왜 바꿨는지 커밋 메시지에 직접 기록하므로 사람이 별도로 커밋 메시지 규칙을 신경 쓸 필요는 없습니다. `git push`는 확인 후 진행됩니다.
4. 이 저장소의 `.claude/` 폴더는 팀 전체가 공유하는 설정입니다. 개인만의 설정이 필요하면 `.claude/settings.local.json`을 만들어 사용하세요 (git에는 올라가지 않습니다).

## 컴포넌트 작업 원칙 — 발견 즉시 보고, 관련 있으면 기본으로 고치기

디자인 시스템 컴포넌트(Button/Input/Textarea/Field/InputGroup 등)를 작업하다가,
지금 다루고 있는 범위와 직접 관련된 미수정 이슈(예: 다른 컴포넌트엔 이미 고친
opacity 트릭이 새 컴포넌트엔 안 고쳐진 경우, 사이즈 누락, 아이콘 크기 누수 등)를
발견하면:

1. 무엇을 발견했는지 먼저 구체적으로 알린다.
2. "요청 안 하셨는데 고쳐도 될까요"로 매번 되묻지 않고, "지금 테스트 범위와
   관련 있으니 기본적으로 고치겠다"는 태도로 제안한다.
3. 실제로 고치기 전에 짧게 확인을 받는다.

완전히 다른 컴포넌트/기능 영역(예: 전혀 별개의 페이지, 이번에 안 건드리는
컴포넌트)까지 스코프를 자동으로 넓히지는 않는다 — "지금 작업 중인 것"이라는
경계는 지킨다.

## 커스텀 명령어 (`.claude/commands/`)

반복되는 작업을 짧은 명령어로 실행할 수 있습니다. 현재는 `/new-component` 하나만 있고(아직 구체적인 컴포넌트 규칙이 없어 최소 절차만 정의됨), 컴포넌트 작업 패턴이 잡히면 팀에서 더 추가할 예정입니다.

## 상태표시줄 (statusLine)

세션 하단에 현재 모델(Sonnet/Opus/Haiku)과 git 브랜치가 항상 표시됩니다 (`.claude/settings.json`의 `statusLine`). 지금 어떤 모델로 작업 중인지 헷갈릴 때 확인하세요.
